apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "platform-ws.fullname" . }}-test-connection"
  labels:
    {{- include "platform-ws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "platform-ws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
