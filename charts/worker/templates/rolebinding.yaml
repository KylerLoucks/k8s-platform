apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "platform-worker.fullname" . }}
  labels:
    {{- include "platform-worker.labels" . | nindent 4 }}
subjects:
  - kind: ServiceAccount
    name: {{ include "platform-worker.serviceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "platform-worker.fullname" . }}
