apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "platform-worker.fullname" . }}
  labels:
    {{- include "platform-worker.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "platform-worker.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "platform-worker.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        checksum/meta: {{ include (print $.Template.BasePath "/meta-configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- toYaml .Values.podAnnotations | nindent 8 }}
    spec:
      serviceAccountName: {{ include "platform-worker.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ include "platform-worker.name" . }}
          image: "{{ include "platform-worker.image" . }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: {{ .Values.probe.liveness.path }}
              port: {{ .Values.probe.liveness.port }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probe.readiness.path }}
              port: {{ .Values.probe.readiness.port }}
          envFrom:
            - configMapRef:
                name: {{ include "platform-worker.fullname" . }}
            - secretRef:
                name: {{ include "platform-worker.fullname" . }}
          volumeMounts:
            - name: meta
              mountPath: /app/meta
              readOnly: true
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: meta
          configMap:
            name: {{ include "platform-worker.fullname" . }}-meta
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}
      tolerations:
        {{- toYaml .Values.tolerations | nindent 8 }}
      affinity:
        {{- toYaml .Values.affinity | nindent 8 }}
