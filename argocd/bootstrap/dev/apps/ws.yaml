apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-ws
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/kylerloucks/k8s-platform.git
      targetRevision: HEAD
      path: charts/ws
      helm:
        valueFiles:
          - $values/values/ws/dev-us/values.yaml
          - $values/values/ws/dev-us/version.yaml
    - repoURL: https://github.com/kylerloucks/k8s-platform.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dev-ws
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
